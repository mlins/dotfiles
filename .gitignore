# Neovim plugin lock file - let each machine manage its own plugin versions
nvim/.config/nvim/lazy-lock.json

# IDE/Editor files
# .cursor/ - removed to allow tracking cursor rules

# Backup files
*.bak
